# Secrets — never commit real keys
.env

# Python bytecode / caches
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv/
venv/
env/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# Locally generated reference PNGs are regenerated by scripts/prepare_reference.py.
# We DO commit references/<id>/details.json and the curated source images, so those
# are NOT ignored here — only stray local scratch output would be added manually.
